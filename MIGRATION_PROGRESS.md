# ClawX Feature2 功能迁移进度

**开始时间**: 2026-04-13  
**目标**: 将 Cartoon 分支的前端功能迁移到 Feature2 分支

---

## ✅ 已完成

### 1. 右键菜单功能 (ContextMenu)
**优先级**: 🔴 高  
**状态**: ✅ 完成

**修改文件**:
- ✅ 创建 `src/components/common/ContextMenu.tsx` (148 行)
- ✅ 修改 `src/App.tsx` (添加 ContextMenu 引入和渲染)

**功能**:
- 全局右键菜单支持
- 复制、粘贴、剪切等标准操作
- 自定义菜单项
- 键盘快捷键支持（Ctrl+C/X 显示 toast）
- 位置智能调整（避免超出屏幕）
- 流畅的动画效果

**测试要点**:
- [ ] 在输入框中右键显示完整菜单（剪切、复制、粘贴、全选）
- [ ] 在选中文本时右键显示复制选项
- [ ] 在空白处右键显示刷新选项
- [ ] Ctrl+C/Ctrl+X 显示成功提示
- [ ] ESC 键关闭菜单
- [ ] 点击外部关闭菜单

### 2. 中文文件名乱码修复
**优先级**: 🔴 高  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `electron/gateway/process-launcher.ts` (添加 PowerShell UTF-8 编码修复)

**功能**:
- 修复 Windows 中文系统下 PowerShell 输出 GBK 编码导致的乱码问题
- 自动注入 UTF-8 编码前缀到 PowerShell 命令
- 确保文件名、路径等中文字符正确显示
- 在所有环境下加载 preload（不仅仅是 dev 模式）

**技术细节**:
- 检测 PowerShell 命令（powershell.exe, pwsh.exe 等）
- 在 `-command` 或 `-c` 参数后注入 UTF-8 编码设置
- 注入代码：`[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; $OutputEncoding = [System.Text.Encoding]::UTF8;`

**测试要点**:
- [ ] 上传中文文件名的文件，检查显示是否正常
- [ ] 查看聊天记录中的中文文件名
- [ ] 检查文件卡片中的中文显示
- [ ] 在 Windows 中文系统测试

---

## 🔄 进行中

### 2. Gateway 状态显示
**优先级**: 🔴 高  
**状态**: ✅ 已存在（无需迁移）

**说明**: Feature2 分支的 `ChatInput.tsx` 已经包含了 Gateway 状态显示功能，包括：
- 绿点/红点指示器
- 显示运行状态、端口号、PID
- 实时更新连接状态

---

## 📋 待完成

### 3. 设备节点选择功能
**优先级**: ❌ 已移除  
**状态**: ❌ 用户不需要此功能

**说明**: 用户明确表示不需要此功能，已从迁移列表中移除。

### 4. Token 使用历史性能优化
**优先级**: 🔴 高  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `electron/utils/token-usage.ts` (并发读取优化)
- ✅ 修改 `src/pages/Models/index.tsx` (添加 limit 参数)

**功能**:
- 并发读取文件，提升性能
- 限制读取文件数量（最多 maxEntries * 2 个文件）
- 默认限制为 200 条记录（避免读取所有文件）
- 统一排序和截取逻辑

**测试要点**:
- [ ] 打开 Models 页面，查看 Token 使用历史加载速度
- [ ] 检查是否正确显示最近 200 条记录
- [ ] 验证排序是否正确（最新的在前）

### 5. 中文文件名乱码修复
**优先级**: 🔴 高  
**状态**: ✅ 完成（见上方）

### 6. 数字员工同步功能
**优先级**: 🔴 高  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `electron/utils/box-im-sync.ts` (添加 updateAgentIdentityMd 函数)

**功能**:
- 添加 `updateAgentIdentityMd` 函数，自动更新每个 bot 的 IDENTITY.md 文件
- 在 `syncBots()` 函数中，同步完成后为每个 bot 调用此函数
- 使用 bot 的 nickName 作为身份名称
- 如果 IDENTITY.md 已存在，只更新 Name 行；否则创建新文件
- 自动创建 workspace 目录（如果不存在）

**测试要点**:
- [ ] 同步数字员工后，检查 `~/.openclaw/workspace-{agentId}/IDENTITY.md` 是否存在
- [ ] 验证 IDENTITY.md 中的 Name 字段是否为 bot 的 nickName
- [ ] 检查日志中是否有 "Updated IDENTITY.md" 的信息

### 7. 文件卡片点击打开
**优先级**: 🟡 中  
**状态**: ✅ 完成（已存在）

**说明**: Feature2 分支的 `ChatMessage.tsx` 已经包含了文件卡片点击打开功能，包括：
- 点击文件卡片调用 `invokeIpc('shell:openPath', file.filePath)` 打开文件
- 悬停时显示交互样式（`hover:bg-black/10 dark:hover:bg-white/10`）
- 鼠标指针变化（`cursor-pointer`）
- 提示文本（`title="Open file"`）

**测试要点**:
- [ ] 点击文件卡片应该打开文件
- [ ] 悬停时应该显示高亮效果
- [ ] 鼠标指针应该变为手型

### 8. 复制消息换行修复
**优先级**: 🟡 中  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `src/pages/Chat/ChatMessage.tsx` (AssistantHoverBar 组件)

**功能**:
- 修复复制消息时换行符丢失的问题
- 使用 ClipboardItem API 确保跨平台兼容性
- 保留文本中的所有换行符和格式
- 添加错误处理和降级方案

**技术细节**:
- 优先使用 `ClipboardItem` API 创建 Blob 对象
- 降级到 `writeText` 作为备选方案
- 异步处理复制操作，避免阻塞 UI

**测试要点**:
- [ ] 复制包含多行文本的消息，粘贴后检查换行是否保留
- [ ] 复制代码块，检查格式是否正确
- [ ] 在不同浏览器测试（Chrome、Edge、Firefox）
- [ ] 检查复制成功后的绿色勾选提示

### 9. 即时滚动优化
**优先级**: 🟡 中  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `src/hooks/use-stick-to-bottom-instant.ts`

**功能**:
- 优化滚动到底部的性能和流畅度
- 改进流式消息时的滚动体验
- 更好的资源清理和内存管理
- 即时滚动，无可见动画延迟

**技术细节**:
- 将 `resize` 模式从 "smooth" 改为 "instant"，提升流式消息时的响应速度
- 添加 `scrollTimeoutRef` 用于清理超时定时器
- 改进 RAF (requestAnimationFrame) 清理逻辑
- 确保组件卸载时正确清理所有定时器和动画帧

**测试要点**:
- [ ] 发送消息后，聊天窗口立即滚动到底部
- [ ] 流式消息时，窗口持续跟随到底部
- [ ] 切换会话时，新会话立即滚动到底部
- [ ] 调整窗口大小时，滚动位置保持正确

### 10. 会话管理增强
**优先级**: 🟡 中  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `src/components/layout/Sidebar.tsx` (添加搜索、重命名功能)
- ✅ 修改 `src/i18n/locales/en/chat.json` (添加翻译)
- ✅ 修改 `src/i18n/locales/zh/chat.json` (添加翻译)
- ✅ 修改 `src/i18n/locales/ja/chat.json` (添加翻译)

**功能**:
- 会话搜索和过滤 - 实时搜索会话名称和 Agent 名称
- 会话重命名 - 点击编辑图标可重命名会话
- 改进的会话列表 UI - 编辑和删除按钮在悬停时显示
- 搜索框带清除按钮
- 支持 Enter 确认重命名，Esc 取消

**技术细节**:
- 使用 `useMemo` 优化搜索过滤性能
- 搜索支持会话名称和 Agent 名称
- 重命名通过 API 调用 `/api/sessions/rename` 持久化
- 内联编辑模式，无需弹窗
- 完整的多语言支持（英文、中文、日文）

**测试要点**:
- [ ] 在搜索框输入关键词，检查会话列表是否正确过滤
- [ ] 点击会话的编辑图标，修改名称后按 Enter 确认
- [ ] 按 Esc 取消重命名
- [ ] 悬停会话时，编辑和删除按钮应该显示
- [ ] 搜索框的清除按钮（X）应该清空搜索

### 11. 模型选择持久化
**优先级**: 🟡 中  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `src/stores/models.ts` (优化 setCurrentModel 逻辑)
- ✅ 修改 `src/pages/Chat/index.tsx` (添加会话模型恢复)

**功能**:
- 每个会话独立记住选择的模型
- 切换会话时自动恢复该会话的模型选择
- 使用 localStorage 持久化存储
- 新会话自动使用数字员工的默认模型（如果有）
- 降级到全局默认模型（glm-5 或第一个可用模型）

**技术细节**:
- `sessionModels` 存储在 localStorage 的 `clawx-session-models` 键
- 切换会话时调用 `ensureSessionModel` 恢复模型
- 选择模型时立即保存到 localStorage
- 同时更新 Gateway 会话配置
- 支持数字员工的默认模型配置

**测试要点**:
- [ ] 在会话 A 选择模型 X，切换到会话 B 选择模型 Y
- [ ] 切回会话 A，检查是否自动恢复为模型 X
- [ ] 刷新页面，检查模型选择是否保持
- [ ] 创建新会话，检查是否使用默认模型
- [ ] 数字员工会话应该使用其配置的默认模型

### 12. 聊天 UI 优化
**优先级**: 🟡 中  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `src/pages/Chat/index.tsx` (优化布局、间距、颜色)

**功能**:
- 更统一的 UI 风格和视觉层次
- 改进的深色模式支持（使用 accent 颜色）
- 更好的间距和布局（增加 padding，优化消息间距）
- 优化的颜色方案（backdrop-blur、半透明背景）
- 改进的错误提示样式（更显眼的位置和样式）
- 优化的欢迎屏幕（更大的标题，更好的按钮样式）
- 改进的加载指示器（统一的动画和样式）

**技术细节**:
- 使用 `backdrop-blur-sm` 实现毛玻璃效果
- 统一使用 `accent` 颜色系统，自动适配深色模式
- 增加 `animate-in fade-in` 动画，提升交互流畅度
- 优化滚动条样式（`scrollbar-thin`）
- 改进边框和阴影效果
- 统一圆角和间距规范

**测试要点**:
- [ ] 切换深色/浅色模式，检查颜色是否协调
- [ ] 检查消息间距是否合适
- [ ] 错误提示是否显眼且易于关闭
- [ ] 欢迎屏幕的按钮悬停效果
- [ ] 加载指示器的动画是否流畅

### 13. 配额显示改进
**优先级**: 🟡 中  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `src/pages/Models/index.tsx` (优化 token 使用历史显示)

**功能**:
- 更清晰的 token 使用历史卡片样式
- 改进的视觉层次和信息组织
- 更大的指示器圆点（2px → 2.5px）
- 更好的间距和布局
- 优化的成本显示（突出显示，带边框）
- 改进的悬停效果和过渡动画
- 更好的深色模式支持

**技术细节**:
- 使用 `backdrop-blur-sm` 和半透明背景
- 卡片添加 shadow 和 hover:shadow 效果
- 统一使用 `text-foreground/80` 提升可读性
- 成本显示使用 `bg-primary/10` 和 `border-primary/20`
- 优化圆角（rounded-2xl → rounded-xl）
- 改进间距（gap-x-4 → gap-x-5, mt-3 → mt-4）

**测试要点**:
- [ ] 查看 Models 页面的 token 使用历史
- [ ] 检查卡片悬停效果是否流畅
- [ ] 验证成本显示是否突出
- [ ] 切换深色模式检查样式
- [ ] 检查指示器圆点是否清晰可见

### 14. 确认对话框改进
**优先级**: 🟡 中  
**状态**: ✅ 完成

**修改文件**:
- ✅ 修改 `src/components/ui/confirm-dialog.tsx` (改进样式和动画)

**功能**:
- 更好的视觉样式和图标
- 改进的动画效果（fade-in + zoom-in）
- 更强的毛玻璃背景（backdrop-blur-sm）
- 添加图标指示器（AlertCircle / AlertTriangle）
- 点击背景关闭对话框
- 确认按钮显示处理状态
- 更好的深色模式支持

**技术细节**:
- 背景从 `bg-black/50` 改为 `bg-black/60 backdrop-blur-sm`
- 添加 `animate-in fade-in zoom-in-95` 动画
- 圆角从 `rounded-lg` 改为 `rounded-xl`
- 阴影从 `shadow-lg` 改为 `shadow-2xl`
- 添加图标容器（圆形背景）
- 按钮最小宽度 80px，统一尺寸
- 确认中显示 "Processing..."

**测试要点**:
- [ ] 删除会话时检查对话框样式
- [ ] 验证动画是否流畅
- [ ] 点击背景应该关闭对话框
- [ ] 按 ESC 应该关闭对话框
- [ ] 确认按钮在处理时应该显示 "Processing..."

### 13. 数据源管理页面
**优先级**: 🟢 低  
**状态**: ⏳ 待开始

**需要创建的文件**:
- `src/pages/Datasources/index.tsx` (259 行)

**功能说明**:
- 管理外部连接器（微信、智谱等）
- 配置 API 密钥和凭证
- 密码输入组件（显示/隐藏切换）
- 保存和测试连接
- 与 openme Control UI 对齐

**工作量**: 较大，需要创建全新页面和相关逻辑

### 14. 初始化进度页面
**优先级**: 🟢 低  
**状态**: ⏳ 待开始

**需要创建的文件**:
- `src/pages/InitProgress/index.tsx` (165 行)

**功能说明**:
- 首次启动时显示初始化进度
- 进度条和步骤显示
- 实时日志输出
- 动画效果（framer-motion）
- 完成后自动跳转

**工作量**: 中等，需要创建全新页面和 IPC 通信

### 15. 对话加载性能优化
**优先级**: 🟢 低  
**状态**: ⏳ 待开始

**需要修改的文件**:
- `src/stores/chat.ts` (核心逻辑改动)
- `src/stores/chat/helpers.ts`

**功能说明**:
- 优化文件附件提取逻辑
- 添加 `extractFileNamesOnly` 函数
- 添加 `extractDirPathsFromCommand` 函数
- 改进文件路径去重逻辑
- 优化工具调用参数映射

**工作量**: 较大，涉及 chat store 核心逻辑，需要仔细测试

---

## 📝 关于剩余功能

这 3 个功能均为**低优先级**，原因如下：

1. **数据源管理页面** - 全新功能，不影响现有用户体验
2. **初始化进度页面** - 仅在首次启动时显示，非核心功能
3. **对话加载性能优化** - 部分优化已在其他改进中完成，剩余改进为增量优化

**建议**: 
- 优先测试已完成的 12 个功能
- 如果这些低优先级功能确实需要，可以作为独立的迭代任务
- 对话加载性能优化可以通过性能监控确定是否真正需要

---

## 📊 进度统计

- ✅ 已完成: 12 / 15 (80%)
- 🔄 进行中: 0 / 15 (0%)
- ⏳ 待完成: 3 / 15 (20%)
- ❌ 已移除: 1 (设备节点选择功能)

---

## 🎯 下一步计划

### 已完成 (12/15)
1. ✅ 右键菜单功能
2. ✅ 中文文件名乱码修复
3. ✅ Token 使用历史性能优化
4. ✅ 数字员工同步功能
5. ✅ 复制消息换行修复
6. ✅ 即时滚动优化
7. ✅ 会话管理增强
8. ✅ 模型选择持久化
9. ✅ 聊天 UI 优化
10. ✅ 配额显示改进
11. ✅ 确认对话框改进
12. ✅ 文件卡片点击打开（已存在）

### 待完成 - 低优先级 (3/15)
13. ⏳ 数据源管理页面 - 全新页面，约 259 行代码
14. ⏳ 初始化进度页面 - 全新页面，约 165 行代码
15. ⏳ 对话加载性能优化 - chat store 核心逻辑改动

**建议**: 这 3 个低优先级功能可以作为独立任务在后续版本中实现。

---

## 📝 注意事项

1. **测试**: 每完成一个功能后需要测试
2. **提交**: 建议每完成一个功能就提交一次
3. **冲突**: 如遇到代码冲突，优先保留 feature2 的架构
4. **依赖**: 注意检查是否有新的依赖需要安装

---

**最后更新**: 2026-04-13 12:00  
**完成度**: 12/15 (80%)  
**状态**: 核心功能已完成，剩余低优先级功能
